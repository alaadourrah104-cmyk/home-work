//Q5-Declare an integer and a double variable representing your age and your height in meters
//respectively. Then declare a num variable and assign it the sum of the two variables. Print all three
void main() {
  int age = 20;
  double height = 1.56;
  num value = age + height;
  print(value);
  print(age);
  print(height);
}